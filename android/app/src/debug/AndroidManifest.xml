<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required so the Flutter tool can talk to the VM service in debug. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
