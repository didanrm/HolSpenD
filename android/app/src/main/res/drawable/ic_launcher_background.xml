<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors the logo's own gradient (deep teal bottom-left, mint top-right) so
     that whatever shape the launcher masks the icon into, the corners the mask
     reveals blend with the artwork instead of showing a hard seam. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="45"
        android:startColor="#065963"
        android:centerColor="#0C7B72"
        android:endColor="#5DC08B"/>
</shape>
