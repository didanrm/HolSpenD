<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ ignores windowBackground and draws its own splash screen, so the
     white background has to be declared again with the splash-screen API.
     The animated icon is left unset on purpose: the system then uses the
     launcher icon, which already respects the 66% safe zone. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
